{
  "name": "boiler",
  "version": "0.0.1",
  "description": "Boiler plate for Revery libraries/projects",
  "license": "MIT",
  "esy": {
    "build": "dune build -p boiler",
    "buildDev": "refmterr dune build -p boiler"
  },
  "scripts": {
    "example": "esy '@example' x example",
    "format": "esy dune build @fmt --auto-promote",
    "test": "esy '@test' x test"
  },
  "dependencies": {
    "@esy-ocaml/reason": "^3.3.7",
    "@opam/dune": ">= 2.0.1",
    "ocaml": ">= 4.4.0"
  },
  "devDependencies": {
    "@opam/merlin": "*",
    "ocaml": "<= 4.08.0",
    "refmterr": "*"
  }
}
