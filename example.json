{
  "source": "./package.json",
  "override": {
    "build": "dune build -p boiler,example",
    "buildDev": "refmterr dune build -p boiler,example",
    "dependencies": {
      "@reason-native/rely": "^3.1.0"
    },
    "install": [
      "esy-installer example.install"
    ]
  }
}
