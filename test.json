{
  "source": "./package.json",
  "override": {
    "build": "dune build -p boiler,test",
    "buildDev": "refmterr dune build -p boiler,test",
    "dependencies": {
      "@reason-native/rely": "^3.1.0"
    },
    "install": [
      "esy-installer test.install"
    ]
  }
}
